[tool.poetry]
name = "book-service-fastapi"
version = "0.1.0"
description = ""
authors = ["Serhii <serhii.domik@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.116.1"
uvicorn = "^0.35.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
